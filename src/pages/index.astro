---
import Layout from '../layouts/Layout.astro';
import Gallery from '../components/Gallery.astro';
---

<Layout>
  <!-- Hero -->
  <section class="hero">
    <div class="container hero-inner">
      <div class="hero-copy">
        <p class="eyebrow">Custom Pet Portraits</p>
        <h1>Hi, I'm Aidan.</h1>
        <p class="lead">
          I recently started painting custom pet portraits. My background in
          anime and cartoon art influences my style, resulting in portraits
          that combine a pet's individuality with cartoon charm.
        </p>
        <p class="lead">
          I started ACC Paintings to create meaningful, sentimental pieces that
          capture the personality and spirit of the pets people love most.
        </p>
        <div class="hero-actions">
          <a href="/commission" class="btn btn-primary">Commission a portrait</a>
          <a href="/gallery" class="btn btn-ghost">See the gallery</a>
        </div>
      </div>
      <div class="hero-art" aria-hidden="true">
        <span class="blob blob-1"></span>
        <span class="blob blob-2"></span>
        <span class="paw">🐾</span>
      </div>
    </div>
  </section>

  <!-- Gallery preview -->
  <section class="section section-alt">
    <div class="container">
      <div class="section-head">
        <div>
          <p class="eyebrow">Recent work</p>
          <h2>A few favorites</h2>
        </div>
        <a href="/gallery" class="btn btn-ghost">View full gallery</a>
      </div>
      <Gallery limit={6} />
    </div>
  </section>

  <!-- Pricing teaser -->
  <section class="section">
    <div class="container">
      <div class="section-head">
        <div>
          <p class="eyebrow">Simple pricing</p>
          <h2>Priced by number of pets</h2>
        </div>
        <a href="/info" class="btn btn-ghost">Full details & FAQ</a>
      </div>
      <div class="price-grid">
        <div class="card price-card">
          <span class="price-amt">$75</span>
          <span class="price-desc">1 pet</span>
          <span class="price-size">8 × 8 canvas</span>
        </div>
        <div class="card price-card">
          <span class="price-amt">$125</span>
          <span class="price-desc">2 pets</span>
          <span class="price-size">8 × 10 canvas</span>
        </div>
        <div class="card price-card">
          <span class="price-amt">$175</span>
          <span class="price-desc">3 pets</span>
          <span class="price-size">10 × 20 canvas</span>
        </div>
        <div class="card price-card">
          <span class="price-amt">$225</span>
          <span class="price-desc">4 pets</span>
          <span class="price-size">10 × 20 canvas</span>
        </div>
      </div>
      <p class="price-note">
        All prices include shipping & packaging. Capitol Hill, Seattle pickups
        get a local discount.
      </p>
    </div>
  </section>

  <!-- CTA -->
  <section class="section">
    <div class="container">
      <div class="cta card">
        <h2>Ready to commission a portrait?</h2>
        <p>
          Turnaround is currently about 1–2 weeks. Fill out the commission form
          and Aidan will be in touch.
        </p>
        <a href="/commission" class="btn btn-primary">Start a commission</a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    background: linear-gradient(180deg, var(--accent-soft) 0%, var(--cream) 100%);
    border-bottom: 1px solid var(--line);
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 2rem;
    align-items: center;
    padding-block: clamp(3rem, 8vw, 6rem);
  }

  .hero-copy h1 {
    margin-top: 0.2rem;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin-top: 1.8rem;
  }

  .hero-art {
    position: relative;
    min-height: 320px;
    display: grid;
    place-items: center;
  }

  .blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(2px);
  }

  .blob-1 {
    width: 260px;
    height: 260px;
    background: radial-gradient(circle at 30% 30%, #f4a261, var(--accent));
    opacity: 0.85;
    top: 10%;
    left: 15%;
  }

  .blob-2 {
    width: 180px;
    height: 180px;
    background: radial-gradient(circle at 40% 40%, #a8dadc, #457b9d);
    opacity: 0.8;
    bottom: 8%;
    right: 12%;
  }

  .paw {
    position: relative;
    font-size: 5rem;
    z-index: 1;
    filter: drop-shadow(0 8px 16px rgba(0, 0, 0, 0.18));
  }

  .section-head {
    display: grid;
  }

  .section-alt {
    background: var(--cream-deep);
    border-block: 1px solid var(--line);
  }

  .section-head {
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .section-head h2 {
    margin-bottom: 0;
  }

  .price-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.2rem;
  }

  .price-card {
    padding: 1.8rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    text-align: center;
  }

  .price-size {
    font-family: var(--font-display);
    font-size: 1.1rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--ink-soft);
  }

  .price-amt {
    font-family: var(--font-display);
    font-size: 2.4rem;
    font-weight: 700;
    color: var(--accent-deep);
  }

  .price-desc {
    color: var(--ink-soft);
    font-size: 0.95rem;
  }

  .price-note {
    margin-top: 1.2rem;
    text-align: center;
    font-size: 0.92rem;
  }

  .cta {
    padding: clamp(2rem, 5vw, 3.5rem);
    text-align: center;
    background: linear-gradient(135deg, var(--accent-soft), var(--white));
  }

  .cta h2 {
    margin-bottom: 0.5rem;
  }

  .cta p {
    max-width: 46ch;
    margin-inline: auto;
    margin-bottom: 1.6rem;
  }

  @media (max-width: 820px) {
    .hero-inner {
      grid-template-columns: 1fr;
    }
    .hero-art {
      min-height: 220px;
      order: -1;
    }
    .price-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .section-head {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 460px) {
    .price-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
